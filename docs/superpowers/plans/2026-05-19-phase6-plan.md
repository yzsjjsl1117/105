# Phase 6 打磨上线 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 产品详情页 UI 重构 + 全站响应式适配 + SEO + middleware→proxy 迁移 + Vercel 部署准备。

**Architecture:** 产品详情页从卡片式改为图片叙事式（自然茶境风格）。响应式使用统一断点 768px 策略，共享组件优先处理，页面按类型分组适配。SEO 补充全站 metadata + og + sitemap。proxy.ts 替换 middleware.ts。

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind CSS 4, inline styles

---

## 文件结构

```
修改（产品详情页重构）:
  src/app/products/[slug]/page.tsx     # 核心：全部重写

修改（响应式 — 共享组件）:
  src/components/ShopNavbar.tsx         # 移动端收缩导航
  src/components/Navbar.tsx             # 移动端适配
  src/components/Footer.tsx             # 移动端适配
  src/components/ShopLayout.tsx         # 移动端 padding 调整

修改（响应式 — 电商页面）:
  src/app/cart/CartContent.tsx          # 表格 → 卡片式
  src/app/checkout/CheckoutContent.tsx  # 表单全宽
  src/app/payment/PaymentContent.tsx    # 卡片全宽
  src/app/payment/success/PaymentSuccessContent.tsx
  src/app/orders/[id]/OrderDetailClient.tsx
  src/app/account/OrderList.tsx         # 卡片全宽

修改（响应式 — 个人中心 + 认证）:
  src/app/account/AccountContent.tsx    # 导航横滑
  src/components/AccountSidebar.tsx      # 移动端横向 tab
  src/app/account/ProfileForm.tsx       # 全宽表单
  src/app/account/PasswordForm.tsx      # 全宽表单
  src/app/account/AddressList.tsx       # 全宽表单
  src/app/auth/layout.tsx              # 移动端居中
  src/app/auth/login/page.tsx          # 全宽输入框
  src/app/auth/register/page.tsx       # 全宽输入框
  src/app/auth/forgot-password/page.tsx
  src/app/auth/reset-password/page.tsx

修改（响应式 — 后台）:
  src/components/AdminNavbar.tsx        # 移动端收缩
  src/app/admin/products/ProductList.tsx  # 表格横滑
  src/app/admin/orders/AdminOrderList.tsx # 表格横滑

创建（SEO）:
  src/app/sitemap.ts                     # 自动生成 sitemap
  src/app/robots.ts                      # robots.txt

修改（SEO）:
  src/app/layout.tsx                     # og:image, twitter:card
  src/app/cart/page.tsx                  # metadata
  src/app/checkout/page.tsx              # metadata
  src/app/auth/login/page.tsx            # metadata
  src/app/account/page.tsx               # metadata

重命名:
  src/middleware.ts → src/proxy.ts       # 弃用迁移

修改（proxy 迁移影响):
  src/proxy.ts                           # 函数名 middleware → proxy
```

---

### Task 1: 产品详情页 UI 重构

**Files:**
- Modify: `src/app/products/[slug]/page.tsx`

整个文件重写。核心变更：
1. 移除产品特性卡片（icon + 标题 + 描述），改为 4 张纵向排列的大图
2. 移除冲泡方法 4 步卡片，改为 1 张大图
3. 移除储存方法卡片，改为 1 张大图
4. 色调更新：底色 `#FDF8F0`，强调色 `#8B6914` / `#C4953A`
5. 移除 `iconSvgs` 对象和 `dangerouslySetInnerHTML`
6. 保持 ShopLayout 包裹 + 左右分栏首屏布局
7. 产品特性顺序：产地环境 → 外形特征 → 香气特点 → 汤色滋味

- [ ] **Step 1: 重写产品详情页**

```typescript
import { notFound } from "next/navigation";
import Link from "next/link";
import { getProductBySlug } from "@/lib/products";
import type { Metadata } from "next";
import ShopLayout from "@/components/ShopLayout";
import AddToCart from "@/components/AddToCart";

interface Feature {
  icon: string;
  title: string;
  desc: string;
}

interface Brewing {
  water: string;
  ratio: string;
  time: string;
  times: string;
}

export default async function ProductDetailPage({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  const { slug } = await params;
  const product = await getProductBySlug(slug);

  if (!product) notFound();

  const features = (product.features as unknown as Feature[]) || [];
  const brewing = (product.brewing as unknown as Brewing) || {};

  return (
    <ShopLayout>
      <div style={{ background: "#FDF8F0", minHeight: "100vh" }}>
        <div style={{ maxWidth: "1100px", margin: "0 auto", padding: "96px 24px 48px" }}>

          {/* 首屏：左右分栏 */}
          <div style={{ display: "flex", gap: "48px", marginBottom: "80px", flexWrap: "wrap" }}>
            {/* 左：产品大图 */}
            <div style={{ flex: "1 1 400px", minWidth: "300px" }}>
              <div style={{
                aspectRatio: "1", borderRadius: "12px", overflow: "hidden",
                background: "linear-gradient(135deg, rgba(139,105,20,0.08) 0%, rgba(26,58,42,0.04) 100%)",
                padding: "24px 24px 0 24px",
              }}>
                <img
                  src={product.images[0] || "/images/产品图.png"}
                  alt={product.name}
                  style={{ width: "100%", height: "100%", objectFit: "cover", borderRadius: "8px 8px 0 0" }}
                />
              </div>
            </div>

            {/* 右：产品信息 */}
            <div style={{ flex: "1 1 300px", display: "flex", flexDirection: "column", justifyContent: "center", gap: "12px" }}>
              <p style={{ fontSize: "12px", color: "#8B6914", letterSpacing: "3px", textTransform: "uppercase", margin: 0, fontFamily: "var(--font-serif-en)" }}>
                {product.englishName}
              </p>
              <h1 style={{ fontSize: "40px", fontWeight: 700, color: "#1a3a2a", margin: 0, fontFamily: "var(--font-serif-cn)" }}>
                {product.name}
              </h1>
              <p style={{ fontSize: "16px", color: "#5C3D2E", margin: 0 }}>{product.subtitle}</p>

              <div style={{ width: "40px", height: "1px", background: "#C4953A", margin: "8px 0" }} />

              <p style={{ fontSize: "36px", fontWeight: 700, color: "#1a3a2a", margin: 0 }}>¥{Number(product.price).toFixed(0)}</p>
              <p style={{ fontSize: "13px", color: "#888", margin: 0 }}>{product.specs}</p>
              <p style={{ fontSize: "14px", color: "#555", lineHeight: 1.8, margin: "8px 0" }}>{product.description}</p>

              <div style={{ display: "flex", flexDirection: "column", gap: "10px", marginTop: "8px" }}>
                <AddToCart productId={product.id} />
                <Link
                  href="/cart"
                  style={{
                    display: "inline-block", padding: "10px 24px", fontSize: "14px", fontWeight: 600,
                    background: "#fff", color: "#1a3a2a", border: "1px solid #1a3a2a",
                    borderRadius: "6px", textAlign: "center", textDecoration: "none",
                  }}
                >
                  查看购物车
                </Link>
              </div>
            </div>
          </div>

          {/* 产品特性：纵向四张大图 */}
          {features.length > 0 && (
            <div style={{ marginBottom: "80px" }}>
              <div style={{ textAlign: "center", marginBottom: "32px" }}>
                <h2 style={{ fontSize: "28px", fontWeight: 700, color: "#1a3a2a", margin: 0, fontFamily: "var(--font-serif-cn)" }}>
                  产品特性
                </h2>
                <div style={{ width: "40px", height: "1px", background: "#C4953A", margin: "10px auto 0" }} />
              </div>
              <div style={{ display: "flex", flexDirection: "column", gap: "12px" }}>
                {features.map((feature, i) => (
                  <div
                    key={i}
                    style={{
                      aspectRatio: "21/9",
                      borderRadius: "10px",
                      overflow: "hidden",
                      background: "linear-gradient(135deg, #f5efe0, #ede0cc)",
                      display: "flex", alignItems: "flex-end",
                      padding: "20px",
                    }}
                  >
                    <div>
                      <p style={{ fontSize: "18px", fontWeight: 700, color: "#1a3a2a", margin: "0 0 4px", fontFamily: "var(--font-serif-cn)" }}>
                        {feature.title}
                      </p>
                      <p style={{ fontSize: "14px", color: "#6B7F5E", margin: 0 }}>{feature.desc}</p>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          )}

          {/* 冲泡方法：一张大图 */}
          {brewing.water && (
            <div style={{ marginBottom: "80px" }}>
              <div style={{
                aspectRatio: "21/9", borderRadius: "10px", overflow: "hidden",
                background: "linear-gradient(135deg, #efe8d8, #e0d5c0, #d5c8a8)",
                display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center",
                padding: "48px 24px",
              }}>
                <h2 style={{ fontSize: "28px", fontWeight: 700, color: "#1a3a2a", margin: "0 0 16px", fontFamily: "var(--font-serif-cn)" }}>
                  冲泡方法
                </h2>
                <div style={{ width: "40px", height: "1px", background: "#C4953A", marginBottom: "20px" }} />
                <div style={{ display: "flex", gap: "40px", flexWrap: "wrap", justifyContent: "center", fontSize: "14px", color: "#555" }}>
                  <span>水温：<b style={{color:"#1a3a2a"}}>{brewing.water}</b></span>
                  <span>茶水比：<b style={{color:"#1a3a2a"}}>{brewing.ratio}</b></span>
                  <span>时间：<b style={{color:"#1a3a2a"}}>{brewing.time}</b></span>
                  <span>次数：<b style={{color:"#1a3a2a"}}>{brewing.times}</b></span>
                </div>
              </div>
            </div>
          )}

          {/* 储存方法：一张大图 */}
          {product.storage && (
            <div style={{ marginBottom: "80px" }}>
              <div style={{
                aspectRatio: "21/9", borderRadius: "10px", overflow: "hidden",
                background: "linear-gradient(135deg, #ece4d4, #dcd0b8, #d0c4a8)",
                display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center",
                padding: "48px 24px",
              }}>
                <h2 style={{ fontSize: "28px", fontWeight: 700, color: "#1a3a2a", margin: "0 0 16px", fontFamily: "var(--font-serif-cn)" }}>
                  储存方法
                </h2>
                <div style={{ width: "40px", height: "1px", background: "#C4953A", marginBottom: "20px" }} />
                <p style={{ fontSize: "15px", color: "#555", textAlign: "center", maxWidth: "400px", lineHeight: 1.8 }}>
                  {product.storage}
                </p>
              </div>
            </div>
          )}

          {/* 返回链接 */}
          <div style={{ textAlign: "center" }}>
            <Link href="/#products" style={{
              fontSize: "14px", color: "#8B6914", textDecoration: "none",
              display: "inline-flex", alignItems: "center", gap: "6px",
            }}>
              <span>←</span> 返回茶叶系列
            </Link>
          </div>

        </div>
      </div>
    </ShopLayout>
  );
}

export async function generateMetadata({
  params,
}: {
  params: Promise<{ slug: string }>;
}): Promise<Metadata> {
  const { slug } = await params;
  const product = await getProductBySlug(slug);
  if (!product) return { title: "产品未找到" };
  return {
    title: `${product.name} - 瀹岭`,
    description: product.description,
  };
}
```

- [ ] **Step 2: 验证编译通过**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: 提交**

```bash
git add src/app/products/\[slug\]/page.tsx
git commit -m "feat: redesign product detail page with natural tea aesthetic"
```

---

### Task 2: 响应式 — 共享组件适配

**Files:**
- Modify: `src/components/ShopNavbar.tsx` — 移动端收缩 padding + 小屏隐藏部分导航
- Modify: `src/components/Navbar.tsx` — 移动端 padding 调整
- Modify: `src/components/Footer.tsx` — 移动端堆叠布局

核心策略：768px 断点。

- [ ] **Step 1: 适配 ShopNavbar — 移动端收缩**

当前 ShopNavbar 使用硬编码 `padding: 8px 64px 8px 44px`。移动端改为 `16px`。

文件 `src/components/ShopNavbar.tsx`：找到导航栏的 `style` 对象，将 padding 调整为：

```typescript
// 找到 nav style 中的 padding
style={{
  // ... 其他样式
  padding: "8px 64px 8px 44px",
  // ...
}}

// 改为使用 CSS 媒体查询或条件判断。最简单的方式：内联 style 中无法直接用 @media，改用 useEffect 检测窗口宽度。
```

实际上，由于项目使用内联 style，移动端适配方式：在每个需要响应式的组件中，使用 `useState` + `useEffect` + `resize` 事件检测窗口宽度，然后条件渲染不同的 style。

更优雅的方案：在 `globals.css` 中定义少量关键响应式类，供组件内联使用。或者直接在组件的 style prop 中使用条件判断。

考虑到项目现有模式（大量内联 style + Tailwind 4），采用方式：**在 globals.css 中定义关键响应式 CSS 自定义属性**，然后用在内联 style 中。

实际上最简单的方案是：对于关键的容器宽度和 padding，在全局 CSS 中定义：

```css
/* src/app/globals.css 末尾添加 */
@media (max-width: 768px) {
  .shop-container {
    padding-left: 16px !important;
    padding-right: 16px !important;
  }
}
```

但这太 hacky。更好的方案：在每个需要响应式的组件中，用 `useWindowWidth` 自定义 hook。

创建 `src/lib/useBreakpoint.ts`：

```typescript
"use client";

import { useState, useEffect } from "react";

export function useBreakpoint() {
  const [isMobile, setIsMobile] = useState(false);

  useEffect(() => {
    function check() {
      setIsMobile(window.innerWidth <= 768);
    }
    check();
    window.addEventListener("resize", check);
    return () => window.removeEventListener("resize", check);
  }, []);

  return { isMobile };
}
```

然后在 ShopNavbar 中使用：

```typescript
// 在 ShopNavbar 组件内
const { isMobile } = useBreakpoint();

// 导航栏 style：
style={{
  display: "flex", justifyContent: "space-between", alignItems: "center",
  padding: isMobile ? "8px 16px" : "8px 64px 8px 44px",
  // ...
}}
```

但 ShopNavbar 目前不是 "use client" 组件... 让我检查一下。

实际上 ShopNavbar 已经导入了 MiniCart（client 组件），所以它应该已经是 "use client" 或者它作为 client component 工作。

让我重新想策略。对于内联 style 的项目，最好的响应式方案是什么？

**方案：创建 `useBreakpoint` hook，在各客户端组件中使用。**

创建 `src/lib/useBreakpoint.ts`，然后在每个需要响应式的客户端组件中引入并条件渲染。

现在写具体步骤：

创建 `src/lib/useBreakpoint.ts`（工具 hook）：

```typescript
"use client";

import { useState, useEffect } from "react";

export function useBreakpoint() {
  const [isMobile, setIsMobile] = useState(false);

  useEffect(() => {
    function onResize() {
      setIsMobile(window.innerWidth <= 768);
    }
    onResize();
    window.addEventListener("resize", onResize);
    return () => window.removeEventListener("resize", onResize);
  }, []);

  return { isMobile };
}
```

然后在各组件中使用。对于 ShopNavbar：

```typescript
// 在 ShopNavbar 组件顶部添加
import { useBreakpoint } from "@/lib/useBreakpoint";
const { isMobile } = useBreakpoint();

// padding 改为条件：
padding: isMobile ? "8px 16px" : "8px 64px 8px 44px",
// 导航链接文字变小：
fontSize: isMobile ? "12px" : "14px",
```

让我重新组织这个 task，使其更具体。

- [ ] **Step 1: 创建 useBreakpoint hook**

创建 `src/lib/useBreakpoint.ts`：

```typescript
"use client";

import { useState, useEffect } from "react";

const BREAKPOINT = 768;

export function useBreakpoint() {
  const [isMobile, setIsMobile] = useState(false);

  useEffect(() => {
    function onResize() {
      setIsMobile(window.innerWidth <= BREAKPOINT);
    }
    onResize();
    window.addEventListener("resize", onResize);
    return () => window.removeEventListener("resize", onResize);
  }, []);

  return { isMobile };
}
```

- [ ] **Step 2: 适配 ShopNavbar**

读取 `src/components/ShopNavbar.tsx`，确保是 `"use client"`，然后：
- 导入 `useBreakpoint`
- padding 条件切换
- 导航文字做小屏处理

```typescript
// 在文件顶部 import 后添加
import { useBreakpoint } from "@/lib/useBreakpoint";

// 在组件函数内添加
const { isMobile } = useBreakpoint();

// 修改 nav style 的 padding
padding: isMobile ? "8px 16px" : "8px 64px 8px 44px",
```

- [ ] **Step 3: 适配 Navbar（首页导航栏）**

类似 ShopNavbar 的处理方式，读取 `src/components/Navbar.tsx`，添加 `useBreakpoint`，调整 mobile 下的 padding。

- [ ] **Step 4: 适配 Footer**

读取 `src/components/Footer.tsx`，添加 `useBreakpoint`，移动端下 flex 方向改为 column，字号缩小。

- [ ] **Step 5: 适配 ShopLayout**

读取 `src/components/ShopLayout.tsx`，确认 CartProvider 包裹逻辑不变，可以在 ShopLayout 级别不做响应式变化（各子页面自己处理）。

- [ ] **Step 6: 验证编译**

```bash
npx tsc --noEmit
```

- [ ] **Step 7: 提交**

```bash
git add src/lib/useBreakpoint.ts src/components/ShopNavbar.tsx src/components/Navbar.tsx src/components/Footer.tsx
git commit -m "feat: add responsive shared components with useBreakpoint hook"
```

---

### Task 3: 响应式 — 电商页面适配

**Files:**
- Modify: `src/app/cart/CartContent.tsx`
- Modify: `src/app/checkout/CheckoutContent.tsx`
- Modify: `src/app/payment/PaymentContent.tsx`
- Modify: `src/app/payment/success/PaymentSuccessContent.tsx`
- Modify: `src/app/orders/[id]/OrderDetailClient.tsx`
- Modify: `src/app/account/OrderList.tsx`

每个文件改动模式相同：导入 `useBreakpoint`，在移动端下切换样式。

- [ ] **Step 1: 适配 CartContent — 表格 → 卡片式**

读取 `src/app/cart/CartContent.tsx`，添加 `useBreakpoint`。移动端下：
- 表头隐藏
- 每个商品显示为卡片（图在上，名称/数量/小计在下，删除按钮在右上）
- "去结算"栏改为全宽底部固定

关键改动：`isMobile` 条件下渲染不同的 JSX 结构。

```typescript
import { useBreakpoint } from "@/lib/useBreakpoint";

// 在组件内：
const { isMobile } = useBreakpoint();
```

移动端渲染逻辑：

```typescript
if (isMobile) {
  // 移动端卡片式布局
  return (
    <>
      <ShopNavbar />
      <div style={{ paddingTop: "64px", padding: "64px 16px 24px" }}>
        <h2 style={{ fontSize: "20px", fontWeight: 600, color: "#1F2D24", marginBottom: "20px" }}>购物车</h2>
        {items.length === 0 ? (
          // 空购物车（同桌面）
        ) : (
          <>
            {items.map((item) => (
              <div key={item.id} style={{ background: "#fff", borderRadius: "10px", padding: "14px", marginBottom: "12px", boxShadow: "0 1px 4px rgba(0,0,0,0.06)" }}>
                <div style={{ display: "flex", gap: "12px" }}>
                  <div style={{ width: "80px", height: "80px", borderRadius: "8px", overflow: "hidden", background: "#f0ebe0", flexShrink: 0 }}>
                    {item.product?.images?.[0] && <img src={item.product.images[0]} alt="" style={{ width: "100%", height: "100%", objectFit: "cover" }} />}
                  </div>
                  <div style={{ flex: 1 }}>
                    <p style={{ fontSize: "14px", fontWeight: 600, margin: "0 0 4px" }}>{item.product?.name}</p>
                    <p style={{ fontSize: "12px", color: "#888", margin: "0 0 8px" }}>¥{Number(item.product?.price || 0)}</p>
                    <div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
                      <button onClick={() => updateQuantity(item.id, Math.max(1, item.quantity - 1))}
                        style={{ width: "28px", height: "28px", border: "1px solid #d1d5db", borderRadius: "4px", background: "#fff", fontSize: "14px", cursor: "pointer" }}>−</button>
                      <span style={{ fontSize: "14px", fontWeight: 600, minWidth: "20px", textAlign: "center" }}>{item.quantity}</span>
                      <button onClick={() => updateQuantity(item.id, item.quantity + 1)}
                        style={{ width: "28px", height: "28px", border: "1px solid #d1d5db", borderRadius: "4px", background: "#fff", fontSize: "14px", cursor: "pointer" }}>+</button>
                    </div>
                  </div>
                  <div style={{ textAlign: "right" }}>
                    <p style={{ fontSize: "16px", fontWeight: 700, color: "#1a3a2a", margin: "0 0 4px" }}>¥{(Number(item.product?.price) || 0) * item.quantity}</p>
                    <button onClick={() => removeItem(item.id)} style={{ fontSize: "12px", color: "#dc2626", border: "none", background: "none", cursor: "pointer" }}>删除</button>
                  </div>
                </div>
              </div>
            ))}
            <div style={{ position: "fixed", bottom: 0, left: 0, right: 0, background: "#fff", borderTop: "1px solid #e5e7eb", padding: "14px 16px", display: "flex", justifyContent: "space-between", alignItems: "center", zIndex: 10 }}>
              <div>
                <span style={{ fontSize: "12px", color: "#888" }}>共 {totalCount} 件 </span>
                <span style={{ fontSize: "18px", fontWeight: 700, color: "#1a3a2a" }}>¥{totalPrice}</span>
              </div>
              <Link href="/checkout" style={{ padding: "10px 28px", background: "#1a3a2a", color: "#fff", borderRadius: "6px", fontSize: "14px", fontWeight: 600, textDecoration: "none" }}>去结算</Link>
            </div>
            <div style={{ height: "80px" }} />
          </>
        )}
      </div>
    </>
  );
}
```

桌面端保持现有代码不变（用 `!isMobile` 包裹）。

- [ ] **Step 2: 适配 CheckoutContent — 全宽**

添加 `useBreakpoint`：
- 容器 padding 改为 `isMobile ? "80px 16px 24px" : "96px 24px 48px"`
- 地址卡片全宽
- 提交按钮全宽

- [ ] **Step 3: 适配 PaymentContent**

添加 `useBreakpoint`：
- 容器 padding 同上
- 支付方式选择卡片缩小

- [ ] **Step 4: 适配 PaymentSuccessContent + OrderDetailClient + OrderList**

每个添加 `useBreakpoint` + 条件 padding，移动端全宽。

- [ ] **Step 5: 验证编译**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: 提交**

```bash
git add src/app/cart/CartContent.tsx src/app/checkout/CheckoutContent.tsx src/app/payment/ src/app/orders/ src/app/account/OrderList.tsx
git commit -m "feat: add responsive layouts for shop pages"
```

---

### Task 4: 响应式 — 个人中心 + 认证页面适配

**Files:**
- Modify: `src/app/account/AccountContent.tsx`
- Modify: `src/components/AccountSidebar.tsx`
- Modify: `src/app/account/ProfileForm.tsx`
- Modify: `src/app/account/PasswordForm.tsx`
- Modify: `src/app/account/AddressList.tsx`
- Modify: `src/app/auth/layout.tsx`
- Modify: `src/app/auth/login/page.tsx`
- Modify: `src/app/auth/register/page.tsx`
- Modify: `src/app/auth/forgot-password/page.tsx`
- Modify: `src/app/auth/reset-password/page.tsx`

- [ ] **Step 1: 适配 AccountSidebar — 横向标签**

移动端下侧边导航改为横向可滑动的 tab 条：

添加 `useBreakpoint`，移动端时 `<nav>` 改为 `flex-direction: row` + `overflow-x: auto`。

- [ ] **Step 2: 适配 AccountContent — 容器宽度**

`maxWidth` 从 `800px` 改为 `100%`，`padding` 条件缩小。

- [ ] **Step 3: 适配 ProfileForm / PasswordForm / AddressList**

添加 `useBreakpoint`，输入框从固定 `320px` 改为 `width: "100%"`，表单区域全宽。

- [ ] **Step 4: 适配认证页面**

添加 `useBreakpoint`，输入框从 `320px` 改为 `width: "100%"`（在父容器内），按钮宽度也相应调整。

- [ ] **Step 5: 验证编译**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: 提交**

```bash
git add src/app/account/ src/app/auth/ src/components/AccountSidebar.tsx
git commit -m "feat: add responsive layouts for account and auth pages"
```

---

### Task 5: 响应式 — 后台管理页面适配

**Files:**
- Modify: `src/components/AdminNavbar.tsx` — 移动端收缩
- Modify: `src/app/admin/products/ProductList.tsx` — 表格横滑
- Modify: `src/app/admin/orders/AdminOrderList.tsx` — 表格横滑

后台页面优先级较低，做最小适配：导航栏收缩 + 表格 `overflow-x: auto`。

- [ ] **Step 1: 适配 AdminNavbar**

添加 `useBreakpoint`，移动端下 padding 缩小，导航链接间距缩小。

- [ ] **Step 2: 适配 ProductList + AdminOrderList 表格横滑**

在 `<table>` 外层包裹 `div`，添加 `overflowX: "auto"`。

```typescript
<div style={{ overflowX: "auto" }}>
  <table style={{ minWidth: isMobile ? "700px" : "100%", ... }}>
    ...
  </table>
</div>
```

- [ ] **Step 3: 验证编译**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: 提交**

```bash
git add src/components/AdminNavbar.tsx src/app/admin/products/ProductList.tsx src/app/admin/orders/AdminOrderList.tsx
git commit -m "feat: add responsive admin pages"
```

---

### Task 6: SEO 优化

**Files:**
- Create: `src/app/sitemap.ts`
- Create: `src/app/robots.ts`
- Modify: `src/app/layout.tsx` — 添加 Open Graph
- Modify: 各页面 — 补充 metadata

- [ ] **Step 1: 创建 sitemap.ts**

```typescript
import { prisma } from "@/lib/prisma";
import type { MetadataRoute } from "next";

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const baseUrl = process.env.NEXT_PUBLIC_BASE_URL || "https://yueling.com";

  const products = await prisma.product.findMany({
    select: { slug: true, createdAt: true },
  });

  const productUrls = products.map((p) => ({
    url: `${baseUrl}/products/${p.slug}`,
    lastModified: p.createdAt,
    changeFrequency: "monthly" as const,
    priority: 0.8,
  }));

  return [
    { url: baseUrl, lastModified: new Date(), changeFrequency: "weekly", priority: 1 },
    { url: `${baseUrl}/cart`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.3 },
    { url: `${baseUrl}/auth/login`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.1 },
    { url: `${baseUrl}/auth/register`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.1 },
    ...productUrls,
  ];
}
```

- [ ] **Step 2: 创建 robots.ts**

```typescript
import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: "*", allow: "/", disallow: "/admin/" },
    sitemap: "https://yueling.com/sitemap.xml",
  };
}
```

- [ ] **Step 3: 更新根布局 metadata — 添加 Open Graph**

在 `src/app/layout.tsx` 中扩展 `metadata`：

```typescript
export const metadata: Metadata = {
  title: "瀹岭 — 云雾之间的馈赠",
  description: "瀹岭高山绿茶，源自黄山云雾深处，百年制茶传承。品质茶礼，一叶知匠心。",
  openGraph: {
    title: "瀹岭 — 云雾之间的馈赠",
    description: "瀹岭高山绿茶，源自黄山云雾深处，百年制茶传承。",
    type: "website",
    locale: "zh_CN",
    siteName: "瀹岭",
  },
  twitter: {
    card: "summary_large_image",
    title: "瀹岭 — 云雾之间的馈赠",
    description: "瀹岭高山绿茶，源自黄山云雾深处，百年制茶传承。",
  },
};
```

- [ ] **Step 4: 补充各页面 metadata**

为以下页面补充 `metadata` export：

- `src/app/cart/page.tsx`：导出 `metadata: Metadata = { title: "购物车 - 瀹岭" }`
- `src/app/checkout/page.tsx`：`{ title: "确认下单 - 瀹岭" }`
- `src/app/auth/login/page.tsx`：`{ title: "登录 - 瀹岭" }`
- `src/app/account/page.tsx`：`{ title: "个人中心 - 瀹岭" }`

（注意：`"use client"` 组件不能直接 `export const metadata`，需要先在 layout 或 server component 中处理。对于已经是 client 的页面容器，可以在同目录下创建 `layout.tsx` 或转为 server component wrapper。）

对于 `"use client"` 页面（如 CartContent、CheckoutContent），metadata 由父级 `layout.tsx` 或 wrapper server component 提供。检查这些页面的容器：

- `src/app/cart/page.tsx`：目前返回 `<CartContent />`，是 server component，可以直接加 metadata
- `src/app/checkout/page.tsx`：同上
- `src/app/auth/login/page.tsx`：是 `"use client"`，需要在 `src/app/auth/layout.tsx` 中设置通用 metadata，或在 login 目录下创建 `layout.tsx`
- `src/app/account/page.tsx`：server component，可以直接加

实际处理方式：给能加的直接加，client component 的页面在其 `layout.tsx` 中处理。

- [ ] **Step 5: 验证编译**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: 提交**

```bash
git add src/app/sitemap.ts src/app/robots.ts src/app/layout.tsx src/app/cart/page.tsx src/app/checkout/page.tsx src/app/account/page.tsx
git commit -m "feat: add SEO — sitemap, robots, og metadata, page titles"
```

---

### Task 7: middleware → proxy 迁移

**Files:**
- Rename: `src/middleware.ts` → `src/proxy.ts`
- Modify: `src/proxy.ts` — 函数签名调整

极低风险改动。Next.js 16 弃用 `middleware` 命名，改为 `proxy`。

- [ ] **Step 1: 创建 proxy.ts 文件**

复制 `src/middleware.ts` 内容到 `src/proxy.ts`，将函数名从 `middleware` 改为 `proxy`：

```typescript
import { NextRequest, NextResponse } from "next/server";

export default function proxy(req: NextRequest) {
  const { pathname } = req.nextUrl;

  const sessionToken =
    req.cookies.get("authjs.session-token")?.value ||
    req.cookies.get("__Secure-authjs.session-token")?.value;
  const isLoggedIn = !!sessionToken;

  if (isLoggedIn && pathname.startsWith("/auth")) {
    return NextResponse.redirect(new URL("/account", req.url));
  }

  if (!isLoggedIn && (pathname.startsWith("/account") || pathname.startsWith("/admin"))) {
    const callbackUrl = encodeURIComponent(req.url);
    return NextResponse.redirect(
      new URL(`/auth/login?callbackUrl=${callbackUrl}`, req.url)
    );
  }

  return NextResponse.next();
}

export const config = {
  matcher: ["/account/:path*", "/auth/:path*", "/admin/:path*"],
};
```

- [ ] **Step 2: 删除旧 middleware.ts**

```bash
rm src/middleware.ts
```

- [ ] **Step 3: 验证编译 + 构建**

```bash
npx tsc --noEmit && npm run build 2>&1 | tail -3
```

确认中间件弃用警告消失。

- [ ] **Step 4: 提交**

```bash
git add src/proxy.ts && git rm src/middleware.ts
git commit -m "refactor: migrate middleware.ts to proxy.ts (Next.js 16)"
```

---

### Task 8: Vercel 部署准备

**Files:**
- Modify: `next.config.ts` — 确认配置
- 可能修改: `package.json` — 确认构建脚本

- [ ] **Step 1: 检查 next.config.ts**

读取 `src/../next.config.ts`，确认没有不兼容的配置。Vercel 部署 Next.js 16 无需特殊配置。

- [ ] **Step 2: 确认 package.json 构建脚本**

`"build": "next build"` 在 Vercel 中自动调用，无需修改。

- [ ] **Step 3: 环境变量清单整理**

确认以下变量在 `.env` 中已配置，部署时需在 Vercel Dashboard 中设置：
- `DATABASE_URL`
- `AUTH_SECRET`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_KEY`

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "chore: prepare Vercel deployment configuration"
```

---

### Task 9: 集成验证

- [ ] **Step 1: 编译 + 构建**

```bash
npx tsc --noEmit && npm run build
```

- [ ] **Step 2: 启动开发服务器，端到端测试**

```bash
npm run dev
```

测试清单：
1. 产品详情页 — 新 UI 正常，首屏左右分栏，特性/冲泡/储存为图片背景
2. 响应式 — Chrome DevTools 切换到 375px 宽度，逐页检查：
   - 购物车→卡片式，结算栏底部固定
   - 结算→全宽
   - 支付→全宽居中
   - 个人中心→导航横滑
   - 认证页→全宽输入框
   - 后台→表格可横滑
3. SEO — 检查 `http://localhost:105/sitemap.xml` 和 `/robots.txt` 可访问
4. proxy — 启动时不再有 middleware 弃用警告

- [ ] **Step 3: 提交修复（如有）**

---

## 验证清单

- [ ] 产品详情页用新 UI（自然茶境风格），首屏左右分栏
- [ ] 产品特性纵向 4 张大图，顺序：产地→外形→香气→汤色
- [ ] 冲泡方法和储存方法各为一张大图 + 文字
- [ ] `useBreakpoint` hook 正常工作
- [ ] 768px 以下购物车变为卡片式
- [ ] 768px 以下结算/支付/订单页面全宽
- [ ] 768px 以下个人中心导航横滑
- [ ] 768px 以下认证页全宽输入框
- [ ] 后台表格可横滑
- [ ] `/sitemap.xml` 可访问
- [ ] `/robots.txt` 可访问
- [ ] 根布局含 Open Graph 标签
- [ ] 启动时无 middleware 弃用警告
- [ ] `npm run build` 通过
